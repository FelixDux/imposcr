version: '3'
services: 
  test_rust:
    stdin_open: true
    build: 
      context: ./imposclib
      dockerfile: Dockerfile.dev
    volumes: 
      - ./imposclib
  test_python:
    stdin_open: true
    build:
      context: .
      dockerfile: Dockerfile.dev
    # volumes: 
    #   - .:.
    command: ["pytest"]
    # ports: 
    #   - 8000:8000